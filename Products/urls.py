# from rest_framework.routers import DefaultRouter
# from Products.views import 
# router=DefaultRouter()
# router.register(prefix='signup',viewset=Signup ,basename='signup_auth')

# router.register(prefix='login',viewset=Login, basename='login_auth')

# router.register(prefix='Verifyotp',viewset=VerifyOtp, basename='verifyotp_auth')

# router.register(prefix='VerifyForgetOtp',viewset=VerifyForgetOtp,basename='forgetotp_auth')

# router.register(prefix='ForgetPassword',viewset=ForgetPassword,basename='forgetpassword_auth')

# router.register(prefix='update_Password',viewset=UpdatePassword,basename='Updatepassword_auth')

# urlpatterns=router.urls
